>install node js by downloading from web sit
>run below command to install agular cli
	npm install -g @angular/cli
>go to workspace and create project
	npm new dashboard
>go to eclipse-> marketspace-> add anuglar plugin	
>import project	
>npm install
>ng serve

Note: faced one error,ps1 cannot be loaded because running scripts is disabled on this system
Fix: run/windows powershell -> run as administrator -> execute below commad
	>Set-ExecutionPolicy unrestricted